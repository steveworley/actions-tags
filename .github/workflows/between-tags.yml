on:
  - push

jobs:
  logLatestRelease:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: git ls-remote -q -t

      # - name: Last tag
      #   run: echo "last - ${{ steps.last_tag.name }} current - ${{ github.ref }}"
